pipeline {
    agent any
    stages {
        stage('Compilation and Analysis') {
            steps {
                sh 'chmod +x ./gradlew'
                sh './gradlew clean'
                sh './gradlew build'
            }
        }
    }
}